export enum UserType {
  'ADMIN' = 'admin',
  'NORMAL_USER' = 'user',
  'MODERATOR' = 'moderator',
}

export enum Interested {
  'MEN' = 'men',
  'WOMEN' = 'women',
  'BOTH' = 'both',
}

export enum PostType {
  'PUBLIC' = 'Public',
  'PRIVATE' = 'Private',
}
